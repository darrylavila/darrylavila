/**
 * @file
 * Global utilities.
 *
 */
(function($, Drupal) {

  'use strict';

  Drupal.behaviors.campaign_donation = {
    attach: function(context, settings) {

      // Custom code here
      
      // var mb =$('#alignment').text();
      // $('.banner').addClass(mb);

    }
  };

})(jQuery, Drupal);